# Configurações do Banco de Dados MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=sua_senha_aqui
DB_NAME=nome_do_banco

# ============================================================================
# MELHORIA 4: Configuração do Connection Pool
# ============================================================================
# Tamanho do pool de conexões ao banco de dados
# Recomendado: 5-10 para uso normal, 20-30 para muitos usuários simultâneos
# Padrão se não especificado: 5
DB_POOL_SIZE=5

# ============================================================================
# Guia de Dimensionamento do Pool:
# ============================================================================
# - Escritório pequeno (1-3 usuários): DB_POOL_SIZE=5
# - Escola média (5-10 usuários): DB_POOL_SIZE=10
# - Escola grande (15-30 usuários): DB_POOL_SIZE=25
# - Multi-unidades (30+ usuários): DB_POOL_SIZE=40
# 
# Fórmula: pool_size = usuários_simultâneos + 2
# 
# IMPORTANTE: Verificar limite do MySQL (max_connections)
# Para ver: SHOW VARIABLES LIKE 'max_connections';
# ============================================================================
